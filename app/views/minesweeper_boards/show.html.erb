<h1><%= @minesweeper_board.name %></h1>
<p><%= @minesweeper_board.email %></p>
<table>
  <% @minesweeper_board.board.each do |row| %>
    <tr>
      <% row.each do |cell| %>
        <td><%= cell == 'X' ? '💣' : '⬜️' %></td>
      <% end %>
    </tr>
  <% end %>
</table>
<%= link_to "Back to Home", root_path %>

